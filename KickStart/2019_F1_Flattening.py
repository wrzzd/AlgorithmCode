class Flattening():
	def flattening(self, a, k):
		# a: list[int] , k: number of max unequal
		# return number of least change
		if len(a) <= 1: return int(0)
		if k >= len(a) - 1: return int(0)

		

f = Flattening()
t = f.p()
